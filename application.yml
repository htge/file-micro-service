spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: test
    password: 1234
  freemarker:
    template-loader-path: ["classpath:/ftl/"]
security:
  user:
    name: admin
    password: Release@
  basic:
    path: /manage
management:
  security:
    roles: ["ADMIN"]
    enabled: true
  context-path: /manage
server:
  session:
    cookie:
      name: JSESSID
login:
  next-root: http://localhost:8081/dl/
  root-path: http://localhost:8080/auth/
file:
  local-dir: /Users/ge/Desktop
  server-root: /dl/
  authorization: true
  watcher: false
  